import { ParentTaskSearchPipe } from './parent-task-search.pipe';

describe('ParentTaskSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ParentTaskSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
